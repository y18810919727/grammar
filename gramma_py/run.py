import run_sentence

if __name__ == '__main__':
    sentence="Single crystals in the shape of rods of 8 mm diameter and 80 mm length were prepared using the Bridgman method ."
    rs=run_sentence.Run_sentence()
    rs.run(sentence)
